# aierabi operating rules

- 最優先は追加費用を極力かけず、aierabi・既存X・ASP等から日次5,000〜10,000円の安定収益への到達確率を上げること。機能数・美観・エージェント数を目的にしない。
- 開始時は Git 状態 → `docs/PROJECT_STATE.md` → `docs/NEXT_TASKS.md` → `docs/REVENUE_DECISION_FRAMEWORK.md` → 関連コードを読む。重要判断は `docs/DECISIONS.md` を確認する。
- 現在地の正本は PROJECT_STATE、作業順の正本は NEXT_TASKS。過去レポートの推奨を無条件に再実行しない。文書より実装・根拠を優先し、矛盾は明記して解消する。
- 一度に着手する実装は1件。収益または短期の学習への寄与、完了条件、変更範囲を先に示す。最大5件の次タスクを維持する。
- 既存の未コミット変更を保持する。無断で破棄・上書き・pushしない。コミット作者や認証設定を捏造・変更しない。
- 課金、有料契約、affiliate申請・規約同意、credential、銀行・税務情報、main merge、本番deploy、DNS、外部アカウント操作、SNS実投稿は自動実行しない。人間の明示判断・操作に引き渡す。
- ランキング評価ロジック・重大なセキュリティ変更は実装前に人間承認を要する。モデルへの委任は承認の代わりにならない。
- 未確認は `unknown` / `null`。未取得の実績はゼロではない。推計は仮定と明示し、実績ファイルへ混入させない。
- 公式事実は一次情報URL・確認日・対象地域/プランを伴う。検索断片や確認日だけで現行性を保証しない。無料プラン・無料体験・報酬対象CVを分ける。
- 公式情報、独自評価、ASP会員限定情報を分離する。再現根拠のない点数・ランキング・体験談を作成/更新/実測訴求しない。
- ASP限定条件・生の成果帳票・個人情報・秘密情報をGitやクライアントに入れない。ユーザーが共有許可した匿名集計だけを扱う。
- affiliateは人間の承認根拠、対象商品、掲載媒体、URL条件を確認するまで無効のまま。任意URLを `/go` の遷移先にせず、記事との関連と広告表示を守る。
- 草案キューは公開許可ではない。生成スクリプトの上書き範囲を確認し、外部送信しないローカルテストを使う。
- 終了時に `npm run check`、`git diff --check` を実行し、未追跡ファイルも確認する。成功と未検証の限界を分けて報告する。
- 状態・次タスクを更新し、戦略/承認境界の変更理由はDECISIONSへ残す。詳細な分担・エスカレーションは判断フレームワークに従う。
