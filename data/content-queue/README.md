# Content queue

This directory is an approval queue, not a publishing pipeline. Store one JSON record per proposed update or social post. Nothing here is posted, deployed, or used to alter ranking data automatically.

Required workflow: verify primary sources → review factual claims → human approval → manually copy approved content into the appropriate site data file or social tool.
